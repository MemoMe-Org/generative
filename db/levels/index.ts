import { pollLevels } from './poll.level'
import { messageLevels } from './message.level'
import { overallLevels } from './overall.level'

const levels = {
    poll: pollLevels,
    message: messageLevels,
    overall: overallLevels,
}

export default levels